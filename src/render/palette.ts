/**
 * Стилевой замок проекта.
 *
 * Правило одно: база холодная и сдержанная, акцент один и тёплый — оранжевый,
 * цвет дорожной техники. Светится **только** акцент. Всё, что должно попадать
 * в глаз (машины, активные маршруты, выделение), берёт акцент; всё остальное
 * остаётся приглушённым.
 *
 * Намеренно ограниченная палитра — то, что отличает «выглядит намеренно» от
 * «выглядит дёшево». Новые цвета сюда не добавляются без причины: расширение
 * палитры разрушает замок быстрее любой другой ошибки.
 */

export const palette = {
  /** Фон сцены и туман вдали. */
  background: '#0e1116',
  fog: '#0e1116',

  /** Рельеф — почти не читается, служит подложкой. */
  terrain: '#161b22',
  terrainEdge: '#1b212a',

  /** Дороги по классам: чем выше класс, тем светлее. */
  roadFederal: '#39424f',
  roadRegional: '#2b323c',
  roadLocal: '#232932',

  /** Застройка. Разброс по высоте даёт силуэт, разброс по тону — объём. */
  buildingLow: '#2a313b',
  buildingHigh: '#3b4552',

  /** Акцент. Единственный тёплый цвет и единственный, который светится. */
  accent: '#ff7a1a',
  accentDim: '#b8560f',

  /** Интерфейс. */
  text: '#c9d1d9',
  textDim: '#7d8794',
  panel: '#141922',
  panelBorder: '#232b36',
} as const

/**
 * Освещение. Холодный ключевой свет сверху плюс слабая заливка снизу, чтобы
 * тени не проваливались в чёрное.
 */
export const lighting = {
  keyColor: '#b8c7d9',
  keyIntensity: 1.4,
  /** Направление ключевого света — низкое солнце даёт длинные тени и объём. */
  keyPosition: [180, 260, 120] as const,

  fillColor: '#243040',
  fillIntensity: 0.55,

  ambientColor: '#1a2430',
  ambientIntensity: 0.35,
} as const

/** Туман скрывает край карты и создаёт глубину. Значения в километрах. */
export const fogRange = {
  near: 400,
  far: 1400,
} as const

/** Настройки постпроцессинга. Без него сцена читается как дешёвая. */
export const postFx = {
  bloomIntensity: 0.65,
  bloomThreshold: 0.55,
  bloomSmoothing: 0.4,
  vignetteDarkness: 0.55,
  vignetteOffset: 0.25,
  noiseOpacity: 0.035,
} as const
