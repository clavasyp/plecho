/**
 * Стилевой замок проекта.
 *
 * Правило одно: база холодная и сдержанная, акцент один и тёплый — оранжевый,
 * цвет дорожной техники. Светится **только** акцент. Всё, что должно попадать
 * в глаз (машины, активные маршруты, выделение), берёт акцент; всё остальное
 * остаётся приглушённым.
 *
 * Намеренно ограниченная палитра — то, что отличает «выглядит намеренно» от
 * «выглядит дёшево». Новые цвета сюда не добавляются без причины: расширение
 * палитры разрушает замок быстрее любой другой ошибки.
 */

export const palette = {
  /** Фон сцены и туман вдали. */
  background: '#0e1116',
  fog: '#0e1116',

  /** Рельеф — почти не читается, служит подложкой. */
  terrain: '#1b232d',
  terrainEdge: '#222b36',

  /** Дороги по классам: чем выше класс, тем светлее. */
  roadFederal: '#4d5a6b',
  roadRegional: '#3a444f',
  roadLocal: '#2f3640',

  /** Застройка. Разброс по высоте даёт силуэт, разброс по тону — объём. */
  buildingLow: '#39434f',
  buildingHigh: '#5a6a7d',

  /** Акцент. Единственный тёплый цвет и единственный, который светится. */
  accent: '#ff7a1a',
  accentDim: '#b8560f',

  /** Интерфейс. */
  text: '#c9d1d9',
  textDim: '#7d8794',
  panel: '#141922',
  panelBorder: '#232b36',
} as const

/**
 * Освещение. Холодный ключевой свет сверху плюс слабая заливка снизу, чтобы
 * тени не проваливались в чёрное.
 *
 * ЭТО ЗНАЧЕНИЯ ПОЛУДНЯ, а не константы кадра. Свет ведёт время суток
 * (render/sky.ts): к ночным уровням из этой же таблицы значения приходят
 * плавно, полдень — верхняя точка. Ночные уровни лежат рядом намеренно: пара
 * «день — ночь» должна подбираться вместе, врозь она разъезжается по яркости.
 */
export const lighting = {
  keyColor: '#b8c7d9',
  keyIntensity: 2.4,
  /**
   * Направление ключевого света в полдень — запасное значение.
   *
   * Живое направление считает sky.ts от времени суток и сезона; этот вектор
   * остаётся начальным, чтобы первый кадр не успел мигнуть чужим ракурсом, и
   * задаёт наклон «солнце в южной половине неба», от которого пляшет вся дуга.
   */
  keyPosition: [180, 260, 120] as const,

  fillColor: '#243040',
  fillIntensity: 0.7,

  ambientColor: '#1a2430',
  ambientIntensity: 0.55,

  /**
   * Ночные уровни.
   *
   * Ночь тёмная, но не слепая: карту должно быть видно, иначе игрок просто
   * ставит паузу до утра. Ключ падает почти в ноль — луна не лепит объём, — а
   * заливка с рассеянным светом держат силуэты. Главное следствие: единственный
   * светящийся цвет палитры (акцент — фары и рампы) остаётся ночью такой же
   * яркости, что и днём, и потому становится главным источником света в кадре.
   */
  keyNightIntensity: 0.18,
  fillNightIntensity: 0.22,
  ambientNightIntensity: 0.34,
} as const

/**
 * Цвета суток: чем свет и воздух отличаются от полудня.
 *
 * Полуденные значения лежат в `lighting` и `palette` и здесь не повторяются —
 * два имени для одного цвета и есть тот самый второй источник правды.
 *
 * ПОЧЕМУ РАССВЕТ И ЗАКАТ РАЗНЫЕ. Симметричная дуга солнца дала бы одинаковое
 * утро и вечер, а глаз читает их по-разному: утро холодное и розовато-серое,
 * вечер тёплый. Это единственный способ отличить 8 утра от 8 вечера, не глядя
 * на панель времени.
 *
 * ЗАМОК СТИЛЯ НЕ НАРУШЕН. Закатный ключ тёплый, но его насыщенность вдвое ниже
 * акцентной (#dc9a62 против #ff7a1a), и он именно СВЕТ, а не свечение: умножаясь
 * на холодные тёмные альбедо, он даёт приглушённую бронзу. Светится по-прежнему
 * только акцент.
 */
export const daylight = {
  /** Ключ ночью — луна, то есть отражённое солнце: холодное и слабое. */
  keyNight: '#42618a',
  /** Ключ на рассвете — холодная розовая серость. */
  keyDawn: '#caa8a6',
  /** Ключ на закате — приглушённый янтарь. */
  keyDusk: '#dc9a62',

  fillNight: '#162031',
  ambientNight: '#0d1622',

  /**
   * Воздух — цвет неба, тумана и фона канваса одновременно.
   *
   * Ночной воздух не заведён: это `palette.background`. Ночь — то состояние, в
   * котором сцена жила до появления суток, и партия начинается 1 января в 00:00,
   * то есть ровно на нём. Так первый кадр совпадает с начальным значением в JSX,
   * и никакой вспышки на монтировании не будет.
   */
  /*
   * Дневной воздух заметно светлее ночного, и это не вкусовщина.
   *
   * Туман растворяет в нём дальний план, а сам он занимает бо́льшую часть кадра
   * — всё, что выше линии карты. Пока небо и ночью, и днём было одним и тем же
   * почти чёрным тоном, полдень отличался от полуночи только тем, что видно
   * подложку. Верхний предел ставит тональная компрессия: после ACES и
   * виньетки этот тон приходит на экран примерно на трети яркости, то есть
   * заведомо ниже порога блума.
   */
  airDay: '#374757',
  airDawn: '#3a3443',
  airDusk: '#43342b',
  /** Зимняя дневная дымка: снег подсвечивает воздух снизу. */
  airWinter: '#46535f',
} as const

/**
 * Земля по сезонам.
 *
 * Подложка — самая большая поверхность кадра, и сезон читается прежде всего по
 * ней. Зима светлеет резко (снег), остальные три расходятся едва-едва: осень
 * тусклее и холоднее всех, лето суше и уходит в хаки, весна влажная и синеватая.
 *
 * ПОТОЛОК ЯРКОСТИ У ЗИМЫ НЕ СЛУЧАЕН. Порог блума — 0.55 по яркости кадра
 * (postFx.bloomThreshold), и снежное поле светлее этого порога начало бы
 * светиться целиком, отобрав у акцента его единственное право. Отсюда снег
 * «серый день», а не «белый лист».
 */
export const seasonGround = {
  зима: '#4d5a68',
  весна: '#212b32',
  лето: '#262a22',
  осень: '#212528',
} as const

/**
 * Эпоха: 1994 → 2030 от выцветшего к насыщенному.
 *
 * Сдвиг насыщенности всего кадра, а не подмена цветов по одному. Причина
 * простая: цвета лежат в этом файле и читаются восемью компонентами, и попытка
 * пересчитать каждый из них по году развела бы палитру на два источника — тот
 * самый отказ, от которого этот файл и защищает. Грейд же применяется один раз
 * на собранном изображении и одинаково двигает и застройку, и акцент.
 *
 * Значения — вход HueSaturation: 0 — как есть, −1 — серое, +1 — кислотное.
 * Девяностые выцветшие, но не чёрно-белые: акцент обязан остаться акцентом.
 */
export const era = {
  fromYear: 1994,
  toYear: 2030,
  saturationFrom: -0.24,
  saturationTo: 0.14,
} as const

/**
 * Туман скрывает край карты и создаёт глубину. Значения в километрах.
 *
 * Ночная дальность короче дневной: в темноте дальний план тонет раньше, и это
 * не только правда про видимость, но и способ вернуть кадру глубину, которой
 * ночью неоткуда взяться — тени почти пропали вместе с ключевым светом.
 */
export const fogRange = {
  near: 400,
  far: 1400,
  farNight: 1080,
} as const

/** Настройки постпроцессинга. Без него сцена читается как дешёвая. */
export const postFx = {
  bloomIntensity: 0.65,
  bloomThreshold: 0.55,
  bloomSmoothing: 0.4,
  vignetteDarkness: 0.55,
  vignetteOffset: 0.25,
  noiseOpacity: 0.012,
} as const
